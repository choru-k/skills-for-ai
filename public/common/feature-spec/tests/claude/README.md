# feature-spec Claude E2E tests

Claude CLI E2E cases for `feature-spec`.

Run all `feature-spec` E2E cases:

```bash
cd /Users/cheol/Desktop/choru/skills-for-ai
bash scripts/claude-skill-e2e/test-claude-skills.sh --skill feature-spec
```

Run one case:

```bash
bash scripts/claude-skill-e2e/test-claude-skills.sh --case public/common/feature-spec/tests/claude/feature-spec.smoke.e2e.json
```
