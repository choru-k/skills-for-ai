# Pi agents (canonical source)

This directory is the source of truth for Pi subagent definitions.

Runtime wiring:
- `~/.pi/agent/agents` should symlink to this directory
- Managed by `~/dotfiles/symlink.sh --scope ai`

If agent behavior needs to change, edit the `*.md` files here.
