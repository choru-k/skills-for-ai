---
id: setup-repository
description: Clone selected repository into ~/Desktop/clumio/<repo>/main.
status: active
tags: [node, setup]
links:
  - [[return-summary]]
---

# Setup Repository

Create and clone:
1. `mkdir -p ~/Desktop/clumio/<repo>/`
2. `gh repo clone clumio/<repo> ~/Desktop/clumio/<repo>/main`

If already set up, report existing path and skip clone.
